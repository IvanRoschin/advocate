'use client';

import { CategoryForm } from '../components/forms';

const adminPage = () => {
  return (
    <div className="container">
      adminPage
      <CategoryForm />
    </div>
  );
};

export default adminPage;
