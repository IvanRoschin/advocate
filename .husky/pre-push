#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$branch" = "main" ] || [ "$branch" = "dev" ]; then
  echo "Direct push to '$branch' is blocked. Push a feature branch and open a PR instead."
  exit 1
fi

npm run lint:check
npm run typecheck